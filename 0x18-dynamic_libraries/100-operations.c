int add(int a, int b)
{
	return (a + b);
}

int mul(int a, int b)
{
	return (a * b);
}

int sub(int a, int b)
{
	int difference = (a -b);
	return (difference);
}

int div(int a, int b)
{
	int quotient = (a/b);
	return (quotient);
}

int mod(int a, int b)
{
	int rem = (a % b);
	return (rem);
}
